# Unity Game Jam 开发规范

## 适用范围

- 本文件适用于当前仓库根目录及其所有子目录。
- 子目录中的 `AGENTS.md` 可以补充或覆盖本文件在对应目录内的规则。
- 本项目是独立的 Unity Game Jam，不继承其他项目的业务代码、线上服务、热更新或资源发布约束。

## 工作原则

- 默认使用简体中文沟通；先给结论，再说明关键依据、验证结果和未完成事项。
- 开始实现前先明确需求、范围、关键假设和可验证的成功标准。
- 当前阶段优先采用“文档先行”：游戏机制、模块边界、公共契约和验收标准未确认前，不生成具体代码、场景、Prefab 或其他 Unity 工程文件。
- 优先完成一个可运行、可操作、可复现的最小游戏闭环：启动、游玩、胜负或结束、重新开始。
- 采用最小可行实现，避免为一次性需求引入复杂框架、第三方依赖或过度抽象。
- 只修改与当前需求直接相关的文件，保留用户已有改动，不做无关格式化或重构。

## 知识库与并行文档协作

- `KnowledgeBase/README.md` 是知识库入口；新对话窗口先阅读 `KnowledgeBase/00_Project/DocumentIndex.md`，再按任务范围阅读项目概览、系统架构和目标模块。
- `KnowledgeBase/00_Project` 保存项目目标、范围、路线图和未决设计；`01_Architecture` 保存跨模块架构；`02_Modules/<Module>` 保存单模块设计；`03_SharedContracts` 保存跨模块契约；`04_Assets`、`05_Testing`、`06_Decisions`、`07_Changes` 分别保存资源、测试、决策和变更记录。
- 多个对话窗口可以并行编辑不同模块，但每个窗口默认只修改自己认领的模块目录和与之直接相关的项目记录。不得同时编辑同一模块 README 或同一条共享契约。
- 修改共享契约、项目目标、路线图或架构文档前，先在 `KnowledgeBase/06_Decisions` 新增或更新决策记录，并在变更日志登记影响范围；不确定的方案写入设计待决清单，不直接伪装成已确认规则。
- 文档状态与实现状态分开理解：`InDesign`、`ContractReady` 只表示设计成熟度，不代表已有可运行代码；只有用户明确授权工程实现后，才进入 `InProgress`、`InTest`、`Integration` 或 `Done`。
- 文档修改完成后至少检查链接、状态、接口名称和变更记录的一致性；文档任务不要求执行 Unity 编译，但必须说明未进行工程验证。

## 修改授权与文件边界

- 未获得明确授权时，只读检查，不修改脚本、场景、Prefab、资源、配置或 Git 元数据。
- 新增或修改 `.cs` 文件前，应说明文件路径和职责；新增脚本对应的 `.meta` 文件必须一并纳入变更范围。
- 场景、Prefab、材质、动画、ScriptableObject、项目设置等非 `.cs` 文件属于 Unity 资源，应在修改前说明具体节点、字段和目标值。
- 生成文件、带有 `Generated`、`自动生成` 或 `DO NOT EDIT` 标记的文件只能读取；应修改生成源或生成器。
- 必需的组件和资源优先通过 `[SerializeField]` 绑定，不使用 `Find`、`GetComponent`、`AddComponent` 作为缺失引用的静默兜底。

## 目录与代码约定

- 推荐按职责保持简单目录：
  - `Assets/Scripts/Game`：核心玩法和游戏状态。
  - `Assets/Scripts/UI`：界面和交互表现。
  - `Assets/Scripts/Tools`：仅编辑器工具或调试辅助。
  - `Assets/Art`、`Assets/Audio`、`Assets/Prefabs`、`Assets/Scenes`：对应资源。
- 类型、方法和变量命名应清晰表达意图，避免无意义缩写。
- MonoBehaviour 负责 Unity 生命周期和表现；纯 C# 类负责可测试的规则、计分和状态转换。
- 复杂逻辑拆成职责单一的小函数；只有存在真实复用价值时才抽取公共模块。
- 关键逻辑添加简洁中文注释，说明设计意图、约束或容易误解的原因，不复述代码表面行为。
- 状态较多时可使用有限状态机；状态很少时优先使用简单枚举和显式分支。
- 高频生成和销毁的对象（例如子弹、敌人、伤害数字、粒子）达到性能需求后再引入对象池。
- 动画优先使用 Unity Animator、Coroutine 或项目已选用的 Tween 方案，不重复实现多个动画系统。

## Unity 资源与引用

- 场景、Prefab 和 ScriptableObject 的引用必须在 Inspector 中可追踪，避免依赖隐式路径和运行时搜索。
- 资源命名应能表达用途，脚本、Prefab、场景和配置尽量使用一致的功能前缀或目录。
- 变更场景或 Prefab 后检查引用是否丢失、对象层级是否稳定、运行时是否出现重复单例。
- 不为 Game Jam 默认引入 YooAsset、HybridCLR、远程 CDN、SDK、复杂 ECS 或线上埋点系统，除非需求明确需要。

## 输入、流程与 UI

- 明确支持的平台和输入方式；桌面原型优先键鼠，移动端需求再单独适配触摸和安全区。
- 输入处理与玩法规则分离，便于编辑器、键盘和手柄测试。
- UI 交互应具备可见反馈、禁用状态和失败/重试路径；按钮事件尽量在 Inspector 或明确的初始化代码中绑定。
- 单场景小游戏不需要复杂启动流程；使用一个清晰的入口对象即可。只有存在真实加载阶段时才拆分加载任务。

## 验证要求

- 每次修改后执行与风险相称的最小验证：至少检查编译、启动场景、核心操作和结束/重开流程。
- 仅修改知识库文档时，验证重点是链接、路径、状态、接口名称和变更记录一致性，不要求执行 Unity 编译或运行。
- 新增可计算的规则尽量补充 EditMode 测试；无法自动化时，给出可复现的手动验证步骤。
- 验证失败时继续定位并修正，或明确记录阻塞原因，不把未验证内容描述为已验证。
- 构建前确认 Build Settings 中的启动场景、目标平台、分辨率、输入和资源引用。
- Unity 操作可能产生 `Library/`、`Temp/`、`Logs/` 等可重建产物；不要把它们当作业务修改提交。

## Git 与安全

- 如果当前目录存在 Git 仓库，开始写入前检查 `git status --short`，记录既有改动并避免覆盖；没有 Git 元数据时只做文件级变更检查。
- 未经明确要求，不执行提交、推送、合并、变基、重置、历史重写或批量删除。
- 每轮修改后检查目标文件的 diff、diff 格式错误和实际变更范围。
- 禁止在代码、配置、示例或日志中写入真实密钥、密码、Token、证书或私有服务地址。
- `.gitignore`、`.ignore` 等忽略规则用于控制版本和上下文，不构成敏感数据安全边界。

## 完成标准

- 每一处改动都能直接对应需求，且没有引入未要求的系统。
- 对文档任务：结构、职责边界、链接、状态和变更记录一致；未决内容有明确位置，不把假设写成定案。
- 对工程任务：游戏能从启动场景进入核心玩法，并能完成至少一次完整游玩循环。
- 相关编译、测试或手动验证已通过；未完成或未验证项在最终回复中单独列出。
- 最终报告包含修改文件、验证结果、剩余风险，以及需要用户在 Unity Editor 中手动完成的资源操作。
